using ProyectM2.SO;
using UnityEngine;

namespace ProyectM2
{
    public class CurrencyBonus : MonoBehaviour
    {
        bool bonus { get; }
        // DataIntObservable data;

        void Update()
        {
            if (bonus)
            {
                // data.value = 2;
            }
            //else data.value = 1;
        }
    }
}
